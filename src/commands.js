export var commands = {
  sampleCommand: {
    label: "Command Label",
    run: function(pm, param) {
      //do something
    },
    params: [
      {label: "Param", type: "text"}
    ],
    keys: ["Alt-Shift-Mod-Y"]
  }
}
